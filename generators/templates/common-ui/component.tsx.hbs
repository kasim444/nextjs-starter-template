import { Box, Text } from "@chakra-ui/react";
import { I{{pascalCase name}} } from "@interfaces";

const {{pascalCase name}} = (props: I{{pascalCase name}}) => (
    <Box>
        <Text>{{pascalCase name}}</Text>
    </Box>
);

export default {{pascalCase name}};
